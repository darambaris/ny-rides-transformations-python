# Copy to .env.local and source it before running Spark jobs/notebooks.
# macOS dynamic Java home resolution (no hardcoded personal path).
export JAVA_HOME="$(/usr/libexec/java_home)"

# Spark local networking
export SPARK_LOCAL_IP=127.0.0.1
